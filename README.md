# Optical-Filters-Catalog
A filter catalog project that generates a unique QR code and a static HTML page for each filter, making it easy to identify, access, and manage filter information through a simple, shareable web interface.
